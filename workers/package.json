{
  "name": "taskboi-mcp-worker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm test && npm run typecheck && npm run build && npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.2",
    "@cloudflare/workers-types": "5.20260804.1",
    "typescript": "^7.0.2",
    "vitest": "~4.1.10",
    "wrangler": "4.119.0"
  }
}
